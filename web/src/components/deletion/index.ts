/**
 * Deletion-flow primitives (SHR-AUT-005d scheduled deletion confirmation).
 */

export {
  DeletionCountdown,
  splitRemaining,
  type DeletionCountdownParts,
  type DeletionCountdownProps,
} from './DeletionCountdown'

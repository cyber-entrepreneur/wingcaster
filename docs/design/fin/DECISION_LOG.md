# `fin.*` Decision Log

Living log. Append only. Date + decision + rationale + who. Never silently rewrite a prior row.

| Date | ID | Decision | Rationale | Who |
|---|---|---|---|---|
| 2026-08-18 | DL-000 | Stage 0 split: Agent A owns entity model + this log; B owns state machines + txn matrix; C owns concurrency + idempotency; D owns reconciliation + accounting boundary + security | Four agents, eight §128 deliverables. User confirmed "you pick — claim A". File: `STAGE_0_AGENT_SPLIT.md`. | Agent A |
| 2026-08-18 | DL-001 | New financial schema is `fin`. No new feature writes to `commercial.*` until Stage 13 cutover | Rebuild plan non-negotiable. Dual-write + one full billing period of reconciliation before delete. | Architect (plan) / Agent A restates |
| 2026-08-18 | DL-002 | `UNIT_SCALE = 1_000_000`. All credit, lot, hold, limit, and rating quantities are `BIGINT` atomic units. Money is `BIGINT` ISO-4217 minor units (`*_minor`). Never `FLOAT`/`REAL`/`DOUBLE`/`NUMERIC` on a value path; never JS `Number()` for money | Spec §4 + audit 🚫 on `pricing/resolver.js`. Basis points (`*_bps INTEGER`) for rates/VAT. | Agent A |
| 2026-08-18 | DL-003 | `fin.*` primary keys are `UUID` (`gen_random_uuid()` at insert; Stage 1 may switch to UUIDv7). Cross-schema refs to `public.tenants.id` / `public.users.id` stay `TEXT` via dedicated mapping columns | Existing identity uses `TEXT` PKs. Mixing UUID PKs inside `fin` with explicit `public_*_id TEXT` columns avoids silent type coercion and keeps Stage 13 backfill 1:1. | Agent A |
| 2026-08-18 | DL-004 | Every mutable `fin.*` table has `version BIGINT NOT NULL DEFAULT 1` bumped in a BEFORE UPDATE trigger. PATCH requires `If-Match`. Mismatch → `412 PRECONDITION_FAILED` | Audit C-2. Append-only tables (`usage_events`, `ledger_postings`, `rated_usage`, `financial_audit_events`, invoice lines after ISSUE, vendor statement lines after FINALIZE) have **no** `version` and **no** UPDATE privilege. | Agent A |
| 2026-08-18 | DL-005 | `environment TEXT NOT NULL CHECK (environment IN ('LIVE','TEST'))` on every tenant-scoped and every economic table. FKs that cross rows must match environment (trigger or composite FK) | Spec §113. TEST events must be physically unable to hit a LIVE meter/book. | Agent A |
| 2026-08-18 | DL-006 | Single ledger doctrine: `wa_listings.ai_credit_balances` and `commercial.ledger_entries` are both legacy. `fin.lots` + `fin.ledger_postings` are the only value-movement surface after cutover | Audit A-4 / spec §2. Stage 6/7 consume the replacement; Stage 13 retires the shadows. | Agent A |
| 2026-08-18 | DL-007 | `fin.usage_events` stores **facts**, not prices. `casts_charged` / `price_minor` do **not** exist on the raw event. Rating writes `fin.rated_usage`. Ledger writes happen only through `fin.ledger_transactions` | Spec §1 / §6 / §129 "source service submits final credit price". Closes the emitUsageEvent fusion. | Agent A |
| 2026-08-18 | DL-008 | Append-only financial audit: `fin.financial_audit_events` is INSERT/SELECT only for the app role; hash-chain (`prev_hash`, `row_hash`); denormalised `actor_email_snapshot`. `ON DELETE` of actor rows does **not** null attribution | Audit E-3. Agent D (H_SECURITY) specifies REVOKE + RLS; Agent A only shapes the table. | Agent A |
| 2026-08-18 | DL-009 | `fin.usage_events` is LIST-partitioned by `residency_key` (territory/cell code, plus `__platform__`). Unique constraint is `(environment, source_system, source_event_id, residency_key)` so partition key is in the unique index. Conflict behaviour is `DO NOTHING` (ignore), never `DO UPDATE` | Audit A §2 P1 + cells-over-partitions (handover §4.2). Carries the 42P10 lesson forward. | Agent A |
| 2026-08-18 | DL-010 | `public.tenants` remains the identity tenant. `fin.tenants` is the billing projection (`public_tenant_id TEXT UNIQUE`). Holders and billing accounts hang off `fin.tenants`, not `agents.id` | Audit §3.4/§3.5/§3.6. Today's `req.user.id` as tenantId is a documented collapse; Stage 1 must not repeat it. | Agent A |
| 2026-08-18 | DL-011 | Stage 0 addresses **zero** live P0s in code. Findings stay on the register and are scoped to later stages in the audit's Stage 0 resolution | User: audit is a risk register, not a task list. Silent remediations hide the trail. | Agent A |
| 2026-08-18 | DL-012 | Cross-book rule is **strict same-book postings**. Every posting's `book_id` equals its transaction's `book_id`. Cross-book movement is **paired transactions** sharing `pair_id`. `CLEARING` is an `account_type` inside a book, never a posting that references another book. The prior "except CLEARING legs" clause is deleted | M8. Ambiguity becomes a silent I-01/I-02 violation. Agent C must build the transaction matrix on this rule only | Agent A |
| 2026-08-18 | DL-013 | `usage_events.residency_key` **is** `platform_legal_entities.residency_key` (data-residency / cell / LIST partition key). Billing residency is `seller_legal_entity_id` on contracts and invoices. `public.territories.id` is listing/disclosure and is never the partition key; it may inform which legal entity is selected at contract start | A-Q7 closed. Changes FK target and partition key; B/C/D must not reopen | Agent A |
| 2026-08-18 | DL-014 | Ledger tx uniqueness: partial unique `(environment, economic_source_type, economic_source_id, shape)` for FUNDING/HOLD/VOID/CAPTURE/DIRECT_SPEND/EXPIRY/GRANT/MIGRATE. TRANSFER unique is `(environment, economic_source_id, book_id)`. REFUND and ADJUSTMENT are **not** unique on source — replay is `idempotency_keys` only. `pair_id` + `fx_rate_snapshot_id` reserved on `ledger_transactions` | A-Q3 closed. Agent C does not add a uniqueness column | Agent A |
| 2026-08-18 | DL-015 | FX: `fin.fx_rate_snapshots` (rational `rate_bps_num/rate_bps_den`, kinds DAILY_ECB/TRANSACTION/MONTH_AVG). Cross-currency pair-leg txs and their postings require `fx_rate_snapshot_id`. Rounding residual posts to `ADJUSTMENT` with `reason_code='FX_ROUNDING'` on the **destination** tx (still zero-sum). No eighth `account_type` | M3. Spec keeps 7 account types | Agent A |
| 2026-08-18 | DL-016 | `fin.accounting_periods` is the legal-entity SOX close (`OPEN`/`SOFT_CLOSED`/`HARD_CLOSED`). `billing_periods` remains per billing-account. New `accounting_events` against HARD_CLOSED are rejected | M2 | Agent A |
| 2026-08-18 | DL-017 | `tax_snapshots.tax_treatment` is `STANDARD`/`ZERO_RATED`/`EXEMPT`/`OUT_OF_SCOPE`/`REVERSE_CHARGE`. `vat_bps` alone is not a treatment. CHECK: STANDARD ⇒ vat_bps>0; else vat_bps=0 | M4 | Agent A |
| 2026-08-18 | DL-018 | Invoices reserve ZATCA/Peppol columns at ISSUE: `xml_uuid`, `qr_payload`, `prev_invoice_hash` (Fatoora chain, not audit prev_hash), `zatca_submitted_at`, `pdf_a3_hash`, `pdf_storage_url`, `rendered_at`. WC-KSA makes this mandatory in Stage 10, not a later bolt-on | M5 | Agent A |
| 2026-08-18 | DL-019 | `fin.payment_methods` (vault) and `fin.disputes` are Stage 0 tables. Recurring capture/dunning reads the vault. Chargebacks do not rebuild `payments` | M6 | Agent A |
| 2026-08-18 | DL-020 | `price_versions.tiers` and `dimensional_selector` JSONB are removed. Rating uses `fin.price_tiers` and `fin.price_dimensions` | M7 / §1 "anything rated is a real column" | Agent A |
| 2026-08-18 | DL-021 | Usage-event children use **composite FKs** to `(usage_events.id, usage_events.residency_key)`: `corrects_event_id+corrects_residency_key`, `metered_usage_sources`, `vendor_usage_events` | M1. UUID-only FK is the 42P10 class | Agent A |
| 2026-08-18 | DL-022 | `idempotency_keys.expires_at` default 24h. Expired key → `IDEMPOTENCY_KEY_EXPIRED`, not re-execute. T1–T5/T7 table names reserved in A §16b; owning stage fills columns | T6 + follow-up reservations | Agent A |
| 2026-08-18 | DL-023 | Effective-range overlap on `contract_versions`, `price_versions`, `meter_versions`, `vendor_rate_versions` is exactly `EXCLUDE USING gist (<parent_id> WITH =, tstzrange(effective_from, COALESCE(effective_to, 'infinity'::timestamptz)) WITH &&)` | T9 | Agent A |
| 2026-08-18 | DL-024 | Retention classes (A §19): FINANCIAL_7Y, USAGE_3Y, CONTROL_3Y, PSP_VAULT_ACTIVE, DISPUTE_18M, IDEMPOTENCY_24H, OUTBOX_30D, DLQ_30D, HOLD_90D, IDENTITY_MIRROR. Enforcement is Stage 13; the class is binding now | T8 | Agent A |
| 2026-08-18 | DL-025 | TRANSFER pair integrity: `CHECK (pair_id IS NULL OR shape = 'TRANSFER')`; `UNIQUE (pair_id, book_id) WHERE pair_id IS NOT NULL`. Exactly-two-rows-per-`pair_id` is a deferred COMMIT assertion in Agent C's transaction matrix — no new column | R2-1. Blocks a 3-leg pair. Agent C references this row when they write the assertion | Agent A |
| 2026-08-18 | DL-026 | FX stamp mechanism is Agent C's: trigger (or equivalent) `fx_rate_snapshot_id IS NOT NULL OR pair_id IS NULL OR counterpart_book.currency = this.book.currency`. A table CHECK cannot join books; do not leave this as prose | R2-2. Test §18 #10 is the behaviour; this row is the mechanism owner | Agent A |
| 2026-08-18 | DL-027 | GDPR erasure vs 7Y financial retention is A-Q9 (Agent D / H). Invoices and `financial_audit_events` survive 7 years; invoicing-party name/address must be pseudonymised or redirected, not hard-deleted with `public.tenants`. `actor_email_snapshot` already covers audit actors. No new table until D/H picks the boundary | R2-3 | Agent A |
| 2026-08-18 | DL-028 | `payment_methods` reserves SEPA/BACS mandate audit columns: `mandate_signed_at`, `mandate_ip`, `mandate_text_version`, `mandate_status`. Required when `kind = 'MANDATE'`. Stage 8 owns enforcement; Stage 0 only claims the columns so the vault is not rebuilt | R2-4 | Agent A |
| 2026-08-18 | DL-029 | `fin.contract_versions.status TEXT NOT NULL` is `DRAFT` / `ACTIVE` / `SUPERSEDED`. The only legal UPDATEs on a version row are this status and `effective_to` close. DRAFT components may be rewritten; ACTIVE/SUPERSEDED versions are frozen. Do not derive DRAFT from `approved_by_approval_id IS NULL` | A omitted the column; user-required machine cannot persist without it. T9 gist still owns overlap. | Agent B |
| 2026-08-18 | DL-030 | `fin.dunning_cases.status` enum: `OPEN` / `REMINDING` / `REMIND_ESCALATED` / `CREDIT_PAUSED` / `USAGE_SUSPENDED` / `LEGAL` / `WRITE_OFF_REVIEW` / `CURED` / `WRITTEN_OFF` / `CANCELED`. Maps 1:1 to A §8.5 `dunning_steps.step_kind` after OPEN. Terminal: CURED, WRITTEN_OFF, CANCELED | A §8.4 left status to Agent B | Agent B |
| 2026-08-18 | DL-031 | `fin.credit_notes.status` and `fin.debit_notes.status` are `DRAFT` / `APPROVED` / `ISSUED` / `VOID`. Notes do not have PART_PAID/PAID. Parent must be an issued invoice | A §10.8 listed `status` without the enum | Agent B |
| 2026-08-18 | DL-032 | `fin.reconciliation_runs.status` is `STARTED` / `RUNNING` / `COMPLETED` / `FAILED` / `CANCELED`. At most one RUNNING per `(environment, scope)` | A §12.6 listed `status` without the enum | Agent B |
| 2026-08-18 | DL-033 | `fin.reconciliation_resolution.status TEXT NOT NULL` is `OPEN` / `PENDING_APPROVAL` / `APPLIED` / `REJECTED`. `WARN` may OPEN→APPLIED without approval. `BLOCK_*` requires `RECONCILIATION_OVERRIDE`. PK is `id UUID` (A listed columns without declaring PK; same convention as every other table) | A omitted status; B needs a machine, not a nullable `resolved_at` as a boolean | Agent B |
| 2026-08-18 | DL-034 | `GRANT` always uses `economic_source_type='APPROVAL_REQUEST'`. Below-threshold grants insert an approval row that SYSTEM auto-executes (`approval_actions.decision='AUTO'`). Do **not** invent `fin.grants`. Bonus units on `FundPurchase` ride **inside** the FUNDING tx as a second lot (`consideration_minor=0`), not a second GRANT | A said “APPROVAL_REQUEST or grant id”. A grant table would be a parallel table. Bonus-as-GRANT would burn the unique GRANT slot on a purchase | Agent B |
| 2026-08-18 | DL-035 | `UNIQUE (vendor_id, period_key, environment)` on `fin.vendor_statements`. `FINALIZED` is terminal; corrections are a new statement (distinct period_key suffix or later period), not a reopen | A omitted uniqueness and reopen. Prevents two OPEN statements silently diverging | Agent B |
| 2026-08-18 | DL-036 | `fin.dunning_cases.controls_snapshot JSONB` captures `account_controls` allow_* flags at the first restrictive step (PAUSE_NEW_CREDIT / SUSPEND_USAGE). `CureDunning` / `CancelDunning` restore from the snapshot. Empty snapshot on a restrictive step → `DUNNING_NO_SNAPSHOT` | Without this, CURED would blindly flip flags true and overwrite a human freeze. JSONB is a documented payload, not a rating surface | Agent B |
| 2026-08-18 | DL-037 | R2-1 mechanism (does not replace DL-025's CHECK + UNIQUE): deferred `CONSTRAINT TRIGGER trg_ledger_tx_pair_cardinality` `DEFERRABLE INITIALLY DEFERRED` on `fin.ledger_transactions` asserts `count(*) = 2` per non-null `pair_id` at COMMIT. 1-leg and 3-leg pairs fail with `23514`. Both TRANSFER legs must be inserted in one `transaction(fn)`. SQL lives in `D_CONCURRENCY.md` §9 | R2-1. Agent A reserved the CHECK/UNIQUE and pointed the cardinality assertion at C. This row is that assertion | Agent C |
| 2026-08-18 | DL-038 | R2-2 mechanism (does not replace DL-026's predicate): deferred `CONSTRAINT TRIGGER trg_ledger_tx_fx_stamp` joins both pair-legs' `ledger_books.currency`. If currencies differ, **each** leg must have `fx_rate_snapshot_id IS NOT NULL` and both legs must share the same snapshot id (one economic conversion, DL-015). A table CHECK cannot join books; a BEFORE INSERT cannot see the counterpart — therefore COMMIT-time. SQL lives in `D_CONCURRENCY.md` §10. Same-currency pairs may omit the snapshot. A §18 #10 is the behaviour test; `fx-stamp.test.js` is the mechanism test | R2-2 | Agent C |
| 2026-08-18 | DL-039 | `fin.idempotency_keys.status` CHECK is `IN_FLIGHT / COMPLETED / FAILED / EXPIRED` (A §12.2 omitted EXPIRED). DL-022 remains: `now() >= expires_at` → `IDEMPOTENCY_KEY_EXPIRED`, never re-execute, never replay a COMPLETED body after expiry. Also: UNIQUE must be split — `UNIQUE(environment, tenant_id, key) WHERE tenant_id IS NOT NULL` plus `UNIQUE(environment, key) WHERE tenant_id IS NULL` — so platform-admin keys do not collide-or-fail-open on NULL. No new table | A omitted the fourth status and the NULL-tenant unique. E §2 | Agent C |
| 2026-08-18 | DL-040 | `+occ` / `version` applies to every A §1.1 MUTABLE + INTENT table, including `fin.environments` and `fin.usage_events_dlq` (column tables omitted `+occ`). Exception: `fin.invoice_sequences` stays without `version` — `UPDATE next_n = next_n + 1 … RETURNING` under `FOR UPDATE` is the concurrency control (A §10.2). Do not invent columns elsewhere | A §1.1 vs per-table drift | Agent C |
| 2026-08-18 | DL-041 | Closes A-Q9 / implements the DL-027 reservation. Erasure mode is **pseudonymise-in-place**. FINANCIAL_7Y rows (`invoices`, `rated_usage`, `payments`, `ledger_*`, `accounting_events`, `financial_audit_events`) survive. `actor_email_snapshot` is kept. Buyer name/email/address are pseudonymised; `tax_id` and `jurisdiction` are load-bearing and are not erased (GDPR Art. 17(3)(b) / KSA PDPL). `tenant_id` on those rows is **not** retargeted. A reserved tombstone tenant (`public_tenant_id='__erased__'` per environment) is IDENTITY_MIRROR display only. `legal_hold` on `fin.tenants` blocks erasure. No new table | R2-3 / A-Q9 | Agent D |
| 2026-08-18 | DL-042 | Reserve buyer-identity snapshot columns on `fin.invoices`, `fin.credit_notes`, `fin.debit_notes` so A-Q9 has a place to pseudonymise without joining live `public.tenants`: `buyer_legal_name`, `buyer_email`, `buyer_address_line1`, `buyer_address_line2`, `buyer_city`, `buyer_region`, `buyer_postal_code`, `buyer_country`, `buyer_tax_id`. Snapshotted at ISSUE. Post-ISSUE UPDATE of name/email/address is allowed **only** for the GDPR erasure worker (`action='GDPR_PSEUDONYMISE'`). `buyer_tax_id` and `buyer_country` are not erased | Missing columns for A-Q9 + tax invoice identity. Not a new table | Agent D |
| 2026-08-18 | DL-043 | Reserve on `fin.tenants`: `erasure_status TEXT NOT NULL DEFAULT 'NONE'` (`NONE` / `REQUESTED` / `PSEUDONYMISED` / `BLOCKED_LEGAL_HOLD`), `erased_at TIMESTAMPTZ`, `legal_hold BOOLEAN NOT NULL DEFAULT false`, `legal_hold_reason TEXT`. IDENTITY_MIRROR follows `public.tenants` except while FINANCIAL_7Y documents still cite the row | A-Q9 companion columns | Agent D |
| 2026-08-18 | DL-044 | Confirms A-Q8: `financial_audit_events.row_hash` is SHA-256 of RFC 8785 JCS over the field list in A §12.5. `ip` / `user_agent` are stored and not hashed. Genesis `prev_hash` is 64-zero hex **per environment**. Do not backfill legacy `audit_log` into the chain | A-Q8 | Agent D |
| 2026-08-18 | DL-045 | Reserve on `fin.reconciliation_runs`: `schedule_kind` (`DAILY` / `PER_CLOSE` / `ON_DEMAND`), `advisory_lock_key`, `triggered_by_actor_type`, `triggered_by_actor_id`, `legal_entity_id`, `billing_period_id`, `accounting_period_id`, `source_system` (`recon_v1` / `backfill_v1`) | A §12.6 stub was insufficient for F scheduling | Agent D |
| 2026-08-18 | DL-046 | Reserve on `fin.reconciliation_checks`: `expected_delta_units BIGINT NOT NULL`, `observed_delta_units BIGINT`, `drift_action TEXT`, `advisory_lock_key TEXT NOT NULL`. <!-- OPEN: `vendor_reported_usage` / `vendor_cost_estimates` / `vendor_actual_costs` column lists in A §11.5–11.6 remain stubs; F R080–R086 assume `vendor_id`, `period_key`, `quantity_units`, `amount_minor`, `currency`, `vendor_usage_event_id`. Agent A fills the stubs. --> | F pair contract | Agent D |
| 2026-08-18 | DL-047 | Password history and JWT session registry are **identity**, not `fin.*`. <!-- OPEN: auth stage names `public.password_history` and `public.auth_sessions` (or equivalent). Do not invent `fin.password_history` / `fin.sessions`. --> H §7–§8 is the posture (min 12, HIBP, reuse-5, bcrypt 12, 15m access + rotating refresh + `session_id`, TOTP enable bumps `token_version`) | E5/E6/E8/E9 without a new fin table | Agent D |
| 2026-08-18 | DL-048 | Reserve `fin.approval_requests.min_distinct_approvers INTEGER NOT NULL DEFAULT 1 CHECK (min_distinct_approvers >= 1)`. Kinds `PLATFORM_ADMIN_RECOVERY`, `AUDIT_RETENTION`, and `LARGE_REFUND` against a `PLATFORM` book require `2`. Requester cannot be an `APPROVED` actor. Agent B owns the status machine; this row is the cardinality invariant | E2/E4 + A §12.1 | Agent D |
| 2026-08-18 | DL-049 | Stage 1 `FundPurchase` takes the caller `purchaseIntentId` UUID as `economic_source_id`. It does **not** UPDATE `purchase_intents` (table is Stage 7). Bonus units still ride inside the FUNDING tx (DL-034) | Command-service slice without inventing Stage 7 tables | Stage 1 |
| 2026-08-18 | DL-050 | Stage 1 commands do not enforce `ACCOUNTING_PERIOD_HARD_CLOSED`. `fin.accounting_periods` is not created until Stage 9. Do not invent the table to close the guard | Missing table; B §20 stays scheduled | Stage 1 |
| 2026-08-18 | DL-051 | `WriteOffInvoice` / `IssueDebitNote` / `ReversePayment` (and `IssueCreditNote` without units) are ledger-zero in Stage 1: audit + outbox only, minted UUID as subject. They mint a REFUND/ADJUSTMENT tx only when the caller supplies `bookId` + `units` | Invoice/payment/facility tables are Stage 8/10 | Stage 1 |
| 2026-08-18 | DL-052 | Runner executes F §6 SQL for **R001–R023** verbatim. `R022` / `R023` join `contract_components` / `facility_reservations` (not created). On `42P01` the check row is still inserted with `result='ERROR'`. Do not invent `holds.usage_limit_id` or those tables. R024+ are not run | User-required Stage 1 range; F said R020–R023 go green in Stage 6 | Stage 1 |
| 2026-08-18 | DL-053 | If-Match ships as `fin/middleware/if-match.js` plus a **test-only** PATCH demonstrator on `fin.tenants`. No `/api/admin/fin/**` admin surface (Stage 4/10/12) | D-T12 without stubbing later admin APIs | Stage 1 |
| 2026-08-18 | DL-054 | Lots are created at `remaining_units = granted_units`. `lot_allocations` are strictly draws (−) and restores (+). Issue is **not** an allocation. `insertPostingPair` never stamps `lot_id` on an ISSUANCE-side posting; the AVAILABLE (or other opposite-side) posting may still carry `lot_id` for reporting. R009 source is narrowed to `lot_id IS NOT NULL AND account_type <> 'ISSUANCE' AND shape NOT IN ('FUNDING','GRANT')` so issue AVAILABLE postings without allocations do not false-DRIFT. A §5.3 `remaining = granted + SUM(alloc)` then holds. Option B (recompute trigger + issue alloc) was rejected: after issue it would set remaining = 2×granted and violate `remaining <= granted`. QA note on `feat/stage-1-command-service` after 5bcf315 / 565eda0 / 3218123 | F1 P0 R006 every populated lot drifted | Stage 1 command-service fixup — 2026-08-18 |
| 2026-08-18 | DL-055 | `GrantCredits` against a missing or not-APPROVED/EXECUTED approval throws `APPROVAL_NOT_APPROVED` (APPROVAL). `APPROVAL_FOUR_EYES_REQUIRED` stays reserved for the self-approve / min-distinct-approvers case (B §23 / H7). New code added to B §23 | F6 QA on 565eda0 lineage | Stage 1 command-service fixup — 2026-08-18 |
| 2026-08-18 | DL-056 | `authorization_attempts` is written for AuthorizeHold, CaptureHold, VoidHold, and spend() (A §12.4). `ExpireHold` is a WORKER TTL release and does **not** insert an attempt (no human/authz decision). Capture/Void record `AUTHORIZED` | F8 QA; ExpireHold skip is the Stage 1 choice | Stage 1 command-service fixup — 2026-08-18 |
| 2026-08-19 | DL-057 | `fin.usage_events_dlq` adds the A §6.1 fact columns Stage 1 stubbed as `payload` JSONB: `holder_id`, `billing_account_id`, `event_kind`, `corrects_event_id`, `corrects_residency_key`, `subject_type`, `subject_id`, `quantity_units`, `dimensions`, `occurred_at`, `received_at`, `ingestion_version`. `payload` remains the unstructured dump of the failed attempt. Replay reads columns, not JSON reconstitution | A §6.2 "same fact columns" omitted from 106 | Stage 2 |
| 2026-08-19 | DL-058 | DLQ replay success **DELETE**s the row. Exception to "never DELETE on economic tables": `usage_events_dlq` is MUTABLE companion, not a fact. `GRANT DELETE` to `fin_app_role` | A-2 worker contract; A omitted the success path | Stage 2 |
| 2026-08-19 | DL-059 | Outbox topic `fin.usage.received` is added to B §1. Fired on successful `usage_events` INSERT (not on dedup). `dedupe_key = usage:{residency_key}:{id}` | Stage 2 ingest side-effect; B named `usage.dlq_replay` already | Stage 2 |
| 2026-08-19 | DL-060 | Error code `EVENT_KIND_MISMATCH` (VALIDATION) is added to B §23. ORIGINAL with `correctsEventId`/`correctsResidencyKey` set, or non-ORIGINAL without both, throws `FinError` and does **not** land DLQ | A §6.1 CHECK is the DB backstop; command rejects before INSERT | Stage 2 |
| 2026-08-19 | DL-061 | Missing LIST partition is classified `PARTITION_MISSING` for both `42P01` and PostgreSQL's `23514` "no partition of relation … found for row". `__platform__` is `FOR VALUES IN ('__platform__')`, not a PG DEFAULT catch-all, so unknown keys fail closed into DLQ | Audit A-2; 42P10 lesson | Stage 2 |
| 2026-08-19 | DL-062 | `ensureUsagePartition` takes session `pg_try_advisory_lock(FIN_PARTITION_DDL, hashtext(key))` (class 1011). Loser throws `PARTITION_DDL_IN_PROGRESS` (CONFLICT, retryable). SQL `fin.ensure_usage_events_partition` still uses `pg_advisory_xact_lock` inside the winner | D §7.4 / D-T15 | Stage 2 |
| 2026-08-19 | DL-063 | `meter_versions.filter_definition` JSONB DSL is `{ event_types: string[] (OR), dimensions: { [k]: string \| string[] } (AND; array = IN), excludes: { event_types?, dimensions? } }`. Omitted/empty `event_types` or `dimensions` is no constraint. `excludes` uses the same matcher and is applied only when it has at least one constraint (empty excludes does **not** exclude everything). Unknown keys throw `FIN_FILTER_INVALID`. `evaluateFilter` and `filterToSql` share these semantics | A §6.4 named the column, not the shape | Stage 3 |
| 2026-08-19 | DL-064 | Error codes `FIN_FILTER_INVALID` (VALIDATION), `FIN_METER_VERSION_NOT_FOUND` (PRECONDITION: missing version, or `effective_from > now`), and `METERING_LOCK_HELD` (CONFLICT, retryable) are added to B §23. `METERING_LOCK_HELD` is returned `{ ok:false }`, not thrown — the next tick retries | Stage 3 metering | Stage 3 |
| 2026-08-19 | DL-065 | Outbox topic `fin.metering.completed` is added to B §1. Fired on INSERT of a new ACTIVE `metered_usage` (not on hash-equal dedup). `dedupe_key = metering:{meterVersionId}:{holderId}:{periodKey}:{computationHash}` | Stage 3 side-effect; rating is the consumer (Stage 5) | Stage 3 |
| 2026-08-19 | DL-066 | `TIME_WEIGHTED` aggregation is `SUM(quantity_units * duration_seconds)` where duration is integer seconds from this `occurred_at` to the next event's `occurred_at`, and the last event is weighted to `windowEnd`. Fractional seconds and calendar-aware weights are a Stage 5 nit if rating needs a different grain. CANCELLATION survivors contribute 0 | A §6.4 listed the type without the formula | Stage 3 |
| 2026-08-19 | DL-067 | R035 comparison_query sums `metered_usage_sources` **joined to ACTIVE** `metered_usage` only. F §8's unfiltered `GROUP BY metered_usage_id` would false-DRIFT SUPERSEDED provenance rows (present in sources, absent from the ACTIVE source_query). The invariant is SUM(contributions) ≡ `quantity_units` for ACTIVE rows. `contribution_units` is defined so the equality holds: COUNT → 1 per non-cancelled event; UNIQUE_COUNT → 1 on first `subject_id` (nulls ignored, matching `COUNT(DISTINCT)`); MAX/LATEST → winner quantity, 0 on others; SUM → event quantity; TIME_WEIGHTED → qty×seconds; CANCELLATION effective qty 0 | R035 + R036 must both be GREEN after supersession | Stage 3 |
| 2026-08-19 | DL-068 | `FIN_METERING = 1013`. Per-tuple key is `hashtext(meterVersionId:holderId:periodKey)` on `meterPeriod`. Tick-level coarse lock is `pg_try_advisory_lock(1013, 0)`. The SUPERSEDED status flip is the one legal UPDATE on `metered_usage` (B §0.2): `GRANT UPDATE (status)` + trigger forbidding any other mutation; OCC is `UPDATE … WHERE status='ACTIVE'`. Partial unique index: one ACTIVE row per `(environment, meter_version_id, holder_id, period_key)` | D §7.1 gap; A §6.5 has no `version` column | Stage 3 |
| 2026-08-19 | DL-069 | `fin.price_versions.status` is `DRAFT` / `ACTIVE` / `SUPERSEDED`, matching DL-029. Activate flips DRAFT→ACTIVE and SUPERSEDES the previous ACTIVE in the same tx (closes `effective_to`). Deprecate closes `effective_to` on ACTIVE without a status flip. One ACTIVE per `(environment, price_id)` (partial unique). | A §7.5 omitted status; Stage 4 machine cannot persist without it | Stage 4 |
| 2026-08-19 | DL-070 | DL-023 gist on `price_versions` / `contract_versions` is **partial**: `WHERE (status IN ('ACTIVE','SUPERSEDED'))`. DRAFT rows are excluded so overlap is an activate-time `23P01`, not a draft-time reject. Command maps `23P01`/`23505` → `FIN_PRICE_VERSION_OVERLAP` / `FIN_CONTRACT_VERSION_OVERLAP` | Draft-then-activate lifecycle; audit C-1 success-path test | Stage 4 |
| 2026-08-19 | DL-071 | `fin.contract_components.facility_id` is UUID **without** `REFERENCES fin.credit_facilities`. The parent table is Stage 8. Do not invent `credit_facilities` here | Same missing-table posture as DL-049 / DL-050 | Stage 4 |
| 2026-08-19 | DL-072 | Error codes added to B §23: `BACKDATED_AMENDMENT_REQUIRED` (APPROVAL), `FIN_PRICE_MODEL_INVALID` (VALIDATION), `FIN_PRICE_VERSION_OVERLAP` / `FIN_CONTRACT_VERSION_OVERLAP` (CONFLICT), `FIN_CONTRACT_NO_ACTIVE_VERSION` (PRECONDITION). Outbox topics `fin.price.created` and `fin.price.version` added to B §1 | Stage 4 first `/api/admin/fin/**` surface | Stage 4 |
| 2026-08-19 | DL-073 | `contract_versions` column GRANT is `UPDATE (status, effective_to, approved_by_approval_id)`. B §15 stamps `approved_by_approval_id` on DRAFT→ACTIVE | User-required GRANT was status+effective_to; approval column is the B stamp | Stage 4 |
| 2026-08-19 | DL-074 | R047 / R048 comparison is R037-shaped but restricted to `status IN ('ACTIVE','SUPERSEDED')` so leftover DRAFT ranges do not false-DRIFT. R040–R046 and R049 join `rated_usage` (Stage 5) and stay `ERROR` on `42P01` (DL-052 pattern). R022 becomes GREEN now that `contract_components` exists (empty counters); R023 stays ERROR (`facility_reservations` still missing) | Stage 4 recon range; do not invent `rated_usage` | Stage 4 |
| 2026-08-19 | DL-075 | Stage 4 If-Match is required on every admin **POST** under `/api/admin/fin/prices|contracts` (writeGuards). Create ignores the version number (no row yet). Draft/activate/deprecate/suspend/terminate check the MUTABLE header `version`. D §6.2 still says POST economic commands use Idempotency-Key; this surface is control-plane, not a money command | First real admin consumer of Stage 1 middleware (DL-053) | Stage 4 |
| 2026-08-19 | DL-076 | `adminMutationLimiter` is 10/5m keyed on `req.user.id` with `req.ip` fallback (H §5). It is skipped when `VITEST` is set so `phase-7f3-wiring.test.js` can enumerate routes without 429. Production budget is unchanged | 7f/3 inventory vs E12 limiter | Stage 4 |
| 2026-08-19 | DL-077 | `deprecatePriceVersion` throws `FIN_PRICE_VERSION_NOT_ACTIVE` (PRECONDITION) when the row is missing or not ACTIVE — it does not reuse `FIN_PRICE_VERSION_NOT_DRAFT`. Also listed: `FIN_PRICE_NOT_FOUND` / `FIN_PRICE_VERSION_NOT_FOUND` / `FIN_CONTRACT_NOT_FOUND` / `CONTRACT_VERSION_NOT_DRAFT` / `CONTRACT_ALREADY_TERMINAL` | B §23 completeness for Stage 4 commands | Stage 4 |
| 2026-08-19 | DL-078 | `included_units` is MAX of `INCLUDED_ALLOWANCE.config.included_units` on the same `contract_version` whose `meter_id` equals the metered_usage meter. Never SUM (no double-count). Components with NULL `meter_id` are ignored. For model `INCLUDED_QUANTITY`, also MAX with `price_versions.package_size_units` when that column is set (the price-row N). Amount is then `billable_units * unit_rate_minor` — free units already sit in `included_units` / R045 | A omitted the allowance resolution rule | Stage 5 |
| 2026-08-19 | DL-079 | DIMENSIONAL match: `CHANNEL`→`dimensions.channel`, `TERRITORY`→`territory`, `SEGMENT`→`segment`, `WHATSAPP_CATEGORY`→`whatsapp_category`; `RESIDENCY_KEY` matches `usage_events.residency_key`. The dimensions object is the latest contributing usage_event (`occurred_at DESC, id DESC`). Multiple matches: `dimension_kind` alphabetical, then `dimension_value` lexical; pick first. No match → `unit_rate_minor` if set, else 0 | A named the table, not the selector | Stage 5 |
| 2026-08-19 | DL-080 | Stage 5 hard-codes `late_class='OPEN_PERIOD'`. `billing_period_id` and `accounting_period_id` are reserved UUID columns with **no FK** (Stage 10 / Stage 9 tables). R042 / R043 / R044 / R049 stay `ERROR` on `42P01` (DL-052). Do not invent `billing_periods` / `accounting_periods` / `invoice_lines` here | Missing-table posture | Stage 5 |
| 2026-08-19 | DL-081 | No `fin.rating_dlq` in Stage 5. `FIN_NO_ACTIVE_CONTRACT` / `FIN_NO_ACTIVE_PRICE` leave the `metered_usage` un-rated; the worker logs and continues. Follow-up worker is a later DL | User: do not invent a DLQ without a row | Stage 5 |
| 2026-08-19 | DL-082 | `rating_hash` is SHA-256 of `canonicalJson(explanation)` where `explanation` **is** the preimage: `{meteredUsageId, meteredUsageStatus, contractVersionId, priceVersionId, priceVersionN, model, unit_rate_minor?, package_size_units?, tiers:[sorted], dimensions:[sorted], measured_units, included_units, billable_units, amount_minor}`. BIGINT fields are decimal strings. Optional rate/package keys are omitted when null. R040 comparison is `fin.canonical_json(r.explanation)` vs `rating_hash`, qty from `explanation.amount_minor`. Closes F §9 OPEN | Hasher owned by Stage 5 | Stage 5 |
| 2026-08-19 | DL-083 | `FIN_RATING = 1014`. Per-row lock is `hashtext(meteredUsageId)`; tick lock is `(1014, 0)`. Error codes `FIN_METERED_USAGE_NOT_FOUND` / `FIN_METERED_USAGE_NOT_ACTIVE` / `FIN_NO_ACTIVE_CONTRACT` / `FIN_NO_ACTIVE_PRICE` / `RATING_LOCK_HELD` added to B §23. Outbox `fin.rating.completed` / `rating:{meteredUsageId}:{ratingHash}` added to B §1. Contract resolution: ACTIVE `contract_version` for the tenant whose `billing_accounts.holder_id` matches, effective at `metered_at`; prefer a version with `METER_PRICE` then `OVERAGE_PRICE` for this meter (never both); else `FIN_NO_ACTIVE_CONTRACT`. Missing meter component → `FIN_NO_ACTIVE_PRICE`. Currency is `contracts.billing_currency` (R046); no FX. `occurred_at`/`received_at` are MIN/MAX of contributing usage_events, else `metered_at`. PACKAGE uses integer ceil `(n+d-1)/d`. Re-rate: same hash on any existing row is deduped; a new hash inserts with `adjustment_of_id` = the original (`adjustment_of_id IS NULL`) row | Stage 5 rating engine | Stage 5 |
| 2026-08-20 | DL-084 | `ingestUsageEventWithClient(client, input)` is the client-taking helper. `ingestUsageEvent` still wraps `transaction()` which already joins an ambient tx (`postgres-adapter.js`). Stage 6 `spendCredits` calls the helper so ingest/meter/rate/authorize/capture share one `transaction(fn)` (A/B-1). Logic is not duplicated | User: add a client-passing helper rather than copy ingest | Stage 6 |
| 2026-08-20 | DL-085 | Stage 1 `authorizeHold` is single-lot and inserts `lot_allocations` before `holds`. Migration `119` makes `lot_allocations.hold_id` `DEFERRABLE INITIALLY DEFERRED` so that command COMMITs, and grants `INSERT, UPDATE` on `fin.limit_counters` to `fin_app_role` (Stage 1 was SELECT-only). Stage 6 1-lot plans call `authorizeHold` once; N-lot plans use `write.js` (hold row first, then posting pairs) without editing `transactions.js`. Outer `authorizeUsage` claims the caller key; the inner command uses `{key}:hold` so the two claims do not collide on IN_FLIGHT | Consume Stage 1 without modifying it | Stage 6 |
| 2026-08-20 | DL-086 | Sync metering in `spendCredits`: `meterPeriod` with `period_key = sync:<source_event_id>` and optional `sourceEventId` filter on `usage_events`. Does not supersede the Stage 3 monthly ACTIVE row (R035 still holds per-row). Rating then runs on that metered_usage. `FIN_NO_ACTIVE_CONTRACT` / `FIN_NO_ACTIVE_PRICE` abort the outer tx (no rating DLQ, DL-081) | User: prefer (a) narrow meter of the just-ingested event | Stage 6 |
| 2026-08-20 | DL-087 | Outbox topic `fin.spend.completed` / `spend:{idempotencyKey}:{strategy}` is added to B §1. Reuses `fin.hold.*` for the hold machine; this topic is the product-flow outcome (success or post-rating denial) | User: add only if needed via DL | Stage 6 |
| 2026-08-20 | DL-088 | Authorize two-pass: lock book + accounts, then `SELECT lots … FOR UPDATE` by `(holder_id, status='ACTIVE')` in C §4 order (blocking, not SKIP LOCKED), then `resolveDrawPlan`. `lockLots` on the plan ids is a no-op re-lock in the same tx. Serializes the holder so concurrent 60+60 against 100 cannot double-spend. Facility shortfall is Stage 8; uncovered → `INSUFFICIENT_ELIGIBLE_CREDITS`. NULL `expires_at` is eligible (non-expiring); sort `NULLS LAST` | D §3.4 TOCTOU + user two-pass | Stage 6 |
| 2026-08-20 | DL-089 | `authorizeUsage` throws `IDEMPOTENCY_KEY_REQUIRED` when the caller supplies none of {idempotencyKey, subjectId, ratedUsageId}. The prior silent `randomUUID()` fallback is removed. `spendCredits` always supplies a key so no live path regresses | Direct caller must not lose idempotency silently | Stage 6 |
| 2026-08-20 | DL-090 | Authorize fingerprint now includes `actionKey` / `category` / `vendorId` so an idempotent replay with different applicability matchers surfaces `IDEMPOTENCY_FINGERPRINT_CONFLICT` instead of returning the original response | Applicability drives plan; replay must not route a would-deny into an accept | Stage 6 |
| 2026-08-20 | DL-091 | `FIN_PURCHASE_INTENT = 1015` (per-intent `pg_advisory_xact_lock` inside `transaction(fn)`). `FIN_AUTO_TOPUP` stays **1010** as reserved in D §7.1 and the Stage 1 registry. The Stage 7 prompt's 1016 is not allocated — re-numbering a reserved class would fork D | Worker cluster lock already existed; per-intent class is new | Stage 7 |
| 2026-08-20 | DL-092 | One FUNDING tx per intent (DL-014 restated). Paid + bonus are two lots and four postings inside that FUNDING tx; bonus `source_kind=PROMOTIONAL_GRANT`, `consideration_minor=0`. Stage 7 does **not** call Stage 1 `fundPurchase` (different idempotency key shape); it uses `write.js` primitives on the ambient client | C §5.1 pick / DL-034 | Stage 7 |
| 2026-08-20 | DL-093 | `/api/agent/credits/top-up` and `/api/agency/credits/top-up` are UN-501'd onto `createPurchaseIntent` + `submitPurchasePayment`. Gate is HTTP-only: `FIN_FUNDING_ENABLED` plus a live `fin.tenants` row. commercial.* stays frozen until Stage 13 | User-facing 501s from 7b.1c/17; not `/api/admin/fin/**` | Stage 7 |
| 2026-08-20 | DL-094 | Auto-topup worker MUST NOT charge inline. The ledger tx that trips the threshold (authorize/capture/spend) does not call `submitPurchasePayment`. The worker later inserts a CREATED intent and flips PAYMENT_PENDING, emitting `webhook.stripe` for the after-commit adapter | Spec §52 / B §4; enforced by `auto-topup.test.js` | Stage 7 |
| 2026-08-20 | DL-095 | `refundPurchase` returns `NOT_IMPLEMENTED` (Stage 10 / C §5.7). PAID stays PAID. Do not half-build REFUND txs or `fin.refunds` | User: do not half-build | Stage 7 |
| 2026-08-20 | DL-096 | `171_fin_grants_transfers.sql` creates `fin.credit_products` + `fin.auto_topup_policies`. It does **not** create `fin.grants` (DL-034) or `fin.transfer_intents` (B §26). Auto-topup config is the companion A omitted for spec §52. GrantCredits / TransferCredits remain the Stage 1 commands | Plan filename vs detailed Stage 7 service list | Stage 7 |
| 2026-08-20 | DL-097 | Stage 1 `insertPostingPair` still uses JS unary minus. Stage 7 converts BIGINT through `toSqlInt` and throws above `Number.MAX_SAFE_INTEGER`. Do not edit `write.js` | Consume Stage 1 as-is | Stage 7 |
| 2026-08-20 | DL-098 | Outbound `webhook.stripe` dedupe for submit/void is `stripe:{intentId}:{n}` because `provider_event_id` does not exist yet. Inbound money events use `wh:STRIPE:{provider_event_id}` on `idempotency_keys` plus the permanent UNIQUE on the intent (E §5). Catalog topic `fin.product.created` added to B §1 | B §1 inbound key vs Stage 7 outbound trigger | Stage 7 |
| 2026-08-20 | DL-099 | R057 / R058 are wired in Stage 7 (PAID intent has FUNDING; bonus `consideration_minor=0`). R050–R056 / R059 stay Stage 8 (`payments` / `disputes` / `unapplied_cash` missing). B13 / B22 stay with those tables. B04 is `purchase-intents.test.js` | F §16 Stage 8 range vs purchase_intents landing now | Stage 7 |
| 2026-08-20 | DL-100 | `fin.purchase.status` dedupe is `purchase:{id}:{to}:{version}` so FAILED→PAYMENT_PENDING can write a second PENDING outbox row. B §1 catalogue `purchase:{id}:{to}` is the prefix; version is required by UNIQUE(topic, dedupe_key) | Same `to` twice is a legal B §4 retry | Stage 7 |
| 2026-08-20 | DL-101 | User-facing top-up HTTP (`funding/http.js`) currently accepts `environment` and `now` from `req.body`. A caller MUST NOT pick the intent environment or inject `now`. Server-side tenant context supplies environment; `BusinessClock.now()` supplies time. Today's gate (`FIN_FUNDING_ENABLED` + `fin.tenants` lookup by env) turns a mis-declared env into 501, so this is not a live money bug. Reserved for a Stage 12 hardening pass on the UN-501'd routes. Do not patch in Stage 7 | Audit of PR #12 | Stage 7 |
| 2026-08-20 | DL-102 | `loadBookForAccount` uses `LIMIT 1 ORDER BY id ASC`. Follow-up migration should add `UNIQUE (billing_account_id, environment) WHERE book_type = 'CUSTOMER'`. No live callsite creates a second CUSTOMER book today. Do not patch in Stage 7 | Audit of PR #12 | Stage 7 |
| 2026-08-20 | DL-103 | Funding `withRetry` retries only `40P01` (deadlock), 3 attempts, 20–80ms jitter. `40001` (serialization failure) is not retried. Keep as-is at READ COMMITTED; if isolation is raised to SERIALIZABLE the retry set must grow. Do not patch in Stage 7 | Audit of PR #12 | Stage 7 |
| 2026-08-20 | DL-104 | Advisory lock class `FIN_FACILITY_RESERVATION_EXPIRY = 1017` (mirrors `FIN_HOLD_EXPIRY = 1002`). Do not reuse 1016. Per-facility header lock `FIN_CREDIT_FACILITY = 1018` is xact-scoped inside `transaction(fn)`. | D §7.1 unused-number rule | Stage 8 |
| 2026-08-20 | DL-105 | `FACILITY_DRAW` lots are minted with `granted_units = captured units` and `remaining_units = 0` immediately after capture (negative allocation + `EXHAUSTED`). Spec §122 R052. | Capture remaining-units invariant | Stage 8 |
| 2026-08-20 | DL-106 | Postpaid capture lands ledger-touching steps only: lock reservation, OPEN→CAPTURED, mint `FACILITY_DRAW`, ISSUANCE→CONSUMED via `insertPostingPair`, audit + `fin.facility.reservation` outbox. Invoice / receivable / `accounting_events` rows are Stage 10. | C §5 postpaid 8-step vs Stage 10 | Stage 8 |
| 2026-08-20 | DL-107 | `dunning_cases.controls_snapshot` JSONB is taken at OPEN before the first flag flip and restored on CURED / CANCELED (DL-036 restated). | B §6 + DL-036 | Stage 8 |
| 2026-08-20 | DL-108 | Stage 6 `authorize.js` consults `resolveHybridPlan` when Stage 6 lots do not cover. COMMITTED lots = `source_kind = 'PURCHASE'` AND `contract_id` set. Only Stage 6 file modified; `write.js` / `transactions.js` untouched. | §58 draw order | Stage 8 |
| 2026-08-20 | DL-109 | No `fin.invoices`. `dunning_cases.invoice_id` is UUID without FK. Invoice ISSUED/PART_PAID + `due_at < now` are command arguments. `WRITE_OFF_REVIEW`→`WRITTEN_OFF`, F invoice R052, and F R053 are deferred. R053 is registered and expected `42P01` ERROR. | Stage 10 invoices | Stage 8 |
| 2026-08-20 | DL-110 | A omitted `currency` on `credit_facilities`; added `CHAR(3) NOT NULL`. `limit_minor` is mutable on ACTIVE/PAUSED with `FACILITY_OPS` (no separate amendment machine). | A §8.2 vs B §18 | Stage 8 |
| 2026-08-20 | DL-111 | A omitted `kind`. UNIQUE `(environment, billing_account_id, currency)` — one facility per currency per billing account. | A §8.2 uniqueness | Stage 8 |
| 2026-08-20 | DL-112 | SYSTEM may activate/pause/resume/close without `FACILITY_OPS` (tests/control-plane). USER still requires it. WORKER may suspend without it (dunning USAGE_SUSPENDED). | B §18 actor vs test actor | Stage 8 |
| 2026-08-20 | DL-113 | `authorizeUsage` (Stage 6) reads `input.amountMinor` when it calls `resolveHybridPlan`, but `spend.js` does not compute or forward this value into `authorizeUsage`. Result: the hybrid facility-shortfall path in the spendCredits chain sees `amountMinor: null` and cannot cover a real shortfall. Direct-spend-postpaid tests exercise the reservation path directly, which is why CI did not catch this. Reserved for Stage 10 when the spend↔hybrid wiring is completed alongside invoicing. Do not patch in Stage 8 | Audit of PR #13 | Stage 8 |
| 2026-08-20 | DL-114 | Facility default idempotency key is `FACILITY:{cmdName}:{facilityId}:v{loadedVersion}`. cmdName distinguishes ACTIVATE from RESUME (both target ACTIVE). Loaded facility.version makes each state-flip on a different version its own key so `resume-from-paused` and `resume-from-suspended` never collide. Callers may still pass `idempotencyKey` to override. | Walk PENDING→ACTIVE→PAUSED→ACTIVE→SUSPENDED→ACTIVE reused `FACILITY:ACTIVE:{id}`; the first resume replayed the activate claim and left the row PAUSED. | Stage 8 |
| 2026-08-20 | DL-115 | postpaid CAPTURE inserts matching lot_allocations for every CONSUMED posting with lot_id (R009 semantics unchanged; FACILITY_DRAW lot allocation shape matches PURCHASE lot allocation shape at capture time). | CaptureFacility issues into AVAILABLE (no lot_id — CAPTURE is not excluded from R009) then consumes AVAILABLE→CONSUMED with debitLotId + draw allocation on the AVAILABLE debit so allocation.units = posting.amount_units. Net postings stay ISSUANCE −u / CONSUMED +u (C §5.16); remaining=0 via the draw (DL-105 / R052). Direct ISSUANCE→CONSUMED with creditLotId drifted R009 because CONSUMED amount is +u while the remaining-zeroing allocation is −u. | Stage 8 |
| 2026-08-20 | DL-116 | Advisory lock `FIN_ACCOUNTING_PERIOD_CLOSE = 1019` (xact-scoped per period for SoftClose / HardClose / Reopen). `FIN_ACCOUNTING_CLOSE = 1008` stays the SOX worker class in D §7.1. Do not reuse 1016 (DL-104). | D unused-number rule + Stage 9 plan | Stage 9 |
| 2026-08-20 | DL-117 | Spec §73 restated: `BAD_DEBT_WRITE_OFF` does **not** reverse `REVENUE_RECOGNIZED`. Credit-loss ≠ revenue reversal. `evaluateWriteOff` / `write-off.test.js` enforce this. Ledger CONSUMED postings are not touched. | Spec §73 / C §5.14 / G §2.4 | Stage 9 |
| 2026-08-20 | DL-118 | `fin.accounting_events` is APPEND_ONLY: table-level `REVOKE UPDATE, DELETE, TRUNCATE` from `fin_app_role`. BEFORE INSERT trigger `trg_accounting_events_reject_hard_closed` raises `ACCOUNTING_PERIOD_HARD_CLOSED` when the target period is `HARD_CLOSED` (audit M2 / B20). SOFT_CLOSED inserts are allowed (recon fixes) and flagged by the writer. | A §9.0 / B §550 / user Stage 9 | Stage 9 |
| 2026-08-20 | DL-119 | `fin.accounting_policy_versions` v1 is seeded in migration `192` (`2026-08-18.launch`) with a minimal `ON_CONSUMPTION` recognition rule and `ON_EXPIRY` breakage. A/G stamped `policy_version TEXT`; Stage 9 promotes that pin to a row so events carry `accounting_policy_version_id`. Real jurisdictional policy is ops. No amendment machine — close `effective_to` and insert a new row. | G §1 said do not invent the table; the Stage 9 plan requires the engine + a seed | Stage 9 |
| 2026-08-20 | DL-120 | Stage 9 wires `DEFERRED_REVENUE_CREATED` (Stage 7 `confirmPurchasePayment`), `REVENUE_RECOGNIZED` (Stage 6 `captureUsage` / `spendCredits` DIRECT_SPEND), `RECEIVABLE_CREATED` + `REVENUE_RECOGNIZED` (Stage 8 `captureFacility`), `BREAKAGE_RECOGNIZED` (Stage 9 `expire-lot.js` wrapping Stage 1 `expireLot`), `BAD_DEBT_WRITE_OFF` (`dunning/write-off-invoice.js`). All inserts are inside the parent `transaction(fn)` via ALS reuse (D-T11). No post-commit accounting worker. `transactions.js` / `write.js` / `billing/events.js` untouched. | I-14 / C §5 | Stage 9 |
| 2026-08-20 | DL-121 | `WriteOffInvoice` stubs invoice lookup: `invoice_id` is a UUID argument and `amount_minor` comes from the caller. Fully functional after Stage 10 `fin.invoices`. R061 treats applied payments as **0** until `fin.payments` exists (do not 42P01 / do not half-build Stage 10). Outstanding AR = `SUM(RECEIVABLE_CREATED) − SUM(BAD_DEBT_WRITE_OFF)`. | Missing-table posture | Stage 9 |
| 2026-08-20 | DL-122 | `fin.tax_snapshots.invoice_id` is UUID **without** FK (`fin.invoices` is Stage 10). Stage 9 lands the TABLE + `tax/snapshots.js` writer helper. `resolveTax` is interface-only and is not called from any Stage 9 command. | A §9.3 / Stage 10 ISSUE | Stage 9 |
| 2026-08-20 | DL-123 | `event_kind` CHECK is A's named kinds plus B/C companions the plan lists: `REFUND_REVENUE_REVERSED`, `TRANSFER_INTERNAL`, `ADJUSTMENT_REVENUE`, `FX_REMEASUREMENT`, and A's `CONSIDERATION_ALLOCATED`. Do not invent further kinds. Column is `event_kind` (plan) not A's `event_type`; economic time is `event_at` (A `effective_at`). | A §9.1 vs Stage 9 plan | Stage 9 |
| 2026-08-20 | DL-124 | `revenue_allocation_lines.recognized_amount_minor` + `status` are the only legal UPDATEs on an otherwise APPEND_ONLY companion (ON_CONSUMPTION accumulator). Groups remain INSERT-only. | Plan §9.2 vs A §9.2 | Stage 9 |
| 2026-08-20 | DL-125 | `accounting_periods` keeps A's `starts_at` / `ends_at` / `period_key` (not plan `period_start` / `period_end`). UNIQUE on `(environment, legal_entity_id, period_key)` and `(environment, legal_entity_id, starts_at, ends_at)`. `ever_hard_closed` + `reconciliation_override_approval_id` added so B §550 can forbid SOFT→OPEN after a hard close and stamp the override. Reopen is HARD→SOFT, not HARD→OPEN. | A omitted B columns; B §550 vs plan "HARD→OPEN" | Stage 9 |
| 2026-08-20 | DL-126 | `fin.tax_registrations` is not created. MANUAL `resolveTax` returns a documented in-memory default (`vat_bps=0`, `OUT_OF_SCOPE`). Stage 10 may land the table and read per-legal-entity registrations. | A §16b reserved 9/10; do not half-build | Stage 9 |
| 2026-08-20 | DL-127 | R060–R063 implement the Stage 9 plan restatement (deferred roll-forward, AR, breakage, HARD_CLOSED window), not F §11's original period-equivalence / detective-hard-closed / `effective_at` / policy-pin pair. F's original R061 (no events on a HARD_CLOSED period) would false-DRIFT after a legitimate close. Trigger + `events.test.js` remain the write-time control. | F §11 vs Stage 9 acceptance | Stage 9 |
| 2026-08-20 | DL-128 | `revenue_allocation_lines` may also UPDATE `rated_usage_id` (the ON_CONSUMPTION pointer). R060 joins `REVENUE_RECOGNIZED` to the group via `lines.rated_usage_id` because evaluateConsumption stamps `source_type=RATED_USAGE`, not the group's `PURCHASE_INTENT`. `recognized_amount_minor` + `status` remain the accumulator. Groups stay INSERT-only. | DL-124 + R060 join | Stage 9 |
| 2026-08-20 | DL-129 | R043 flips ERROR→GREEN once `fin.accounting_periods` exists. Its comparison only joins that table; empty `CLOSED_ACCOUNTING` rated_usage is GREEN. R042 still 42P01 (joins `billing_periods`, Stage 10). Sibling `ERROR_CODES` drop R043. | Stage 9 periods vs R04x | Stage 9 |
| 2026-08-20 | DL-130 | Latent Stage 1 defect: `fin.json_canonical(jsonb)` in migration 107 checked `jsonb_typeof(j) = 'bool'`; Postgres returns `'boolean'`. The `'bool'` branch was dead. Every prior stage's audit payloads happened to be boolean-free; Stage 9 periods writeStatus was the first caller to hit it. Fixed by additive migration 195 with `CREATE OR REPLACE FUNCTION`. Existing rows unaffected. Stage 1 file 107 not edited (frozen). JS `idempotency/fingerprint.js` canonicalize already handles `typeof === 'boolean'` (no DL-131). | Audit of PR #14 | Stage 9 |
| 2026-08-20 | DL-131 | Advisory lock `FIN_BILLING_PERIOD_CLOSE = 1020` (xact-scoped per billing period for the 12-step close). `FIN_BILLING_CLOSE = 1007` stays the worker class in D §7.1. Do not reuse 1016 (DL-104). | D unused-number rule + Stage 10 plan | Stage 10 |
| 2026-08-20 | DL-132 | `fin.invoice_sequences` has **no** `version` column and **no** OCC (D §6.3 exception). Concurrency is `SELECT … FOR UPDATE` then `UPDATE next_n = next_n + 1 RETURNING next_n - 1 AS assigned`. VOID keeps `invoice_number` / `note_number` on the row (spec §124); the number is never reused. Sequence tuple is `(environment, legal_entity_id, jurisdiction, doc_type, fiscal_context)` — do not collapse a missing part. Seeds live in test support, not the migration. | A §10.2 / D row 87 | Stage 10 |
| 2026-08-20 | DL-133 | `invoice_payment_allocations.amount_minor` and `payment_allocations.amount_minor` are **sign-flexible** (no `CHECK amount_minor > 0`). Negative rows are compensating entries for `ReversePayment` (C §5.13). `invoice_adjustments.amount_minor` is signed the same way (credits negative, debits positive). | C §5.13 DL note | Stage 10 |
| 2026-08-20 | DL-134 | `fin.unapplied_cash` is command-owned CACHE (recordPayment +, applyPayment −, reversePayment − residual), updated under `FOR UPDATE` in the same `transaction(fn)`. No AFTER INSERT posting trigger: reversals plus sign-flexible allocations would double-count. Stage 1 `account_balances` stays posting-driven because postings are that cache's sole writer. | A §10.11 vs Stage 1 precedent | Stage 10 |
| 2026-08-20 | DL-135 | Stage 7 `refundPurchase` is un-501'd per C §5.7 (this row **lands** DL-095; DL-095 stays as the reserved marker and is not rewritten). Cumulative REFUND-shape amount + prior `REFUND_REVENUE_REVERSED` ≤ `quoted_minor`. LIFO remaining PURCHASE lots first; leftover units mint `REFUND_REVERSAL`. Stage 9 `evaluateRefund` → `REFUND_REVENUE_REVERSED` proportional to recognized revenue. Refund does **not** book credit-loss. PSP path claims `wh:{provider}:{event_id}` first. | DL-095 / C §5.7 | Stage 10 |
| 2026-08-20 | DL-136 | Stage 8 `openDunningCase` reads real `fin.invoices`. Missing / not `ISSUED`/`PART_PAID` / `due_at >= now` → `DUNNING_INVOICE_NOT_ELIGIBLE`. Pre-tx still rejects a caller-supplied bad `invoiceStatus`/`dueAt`. | DL-109 completion | Stage 10 |
| 2026-08-20 | DL-137 | Stage 8 `cureDunning` auto-fires from Stage 10 `applyPayment` when the invoice reaches `PAID`, same `transaction(fn)` / ALS reuse. | B §6 + user Stage 10 wiring | Stage 10 |
| 2026-08-20 | DL-138 | R061 is the full receivable roll-forward (was PARTIAL in Stage 9 / DL-121). Source = `SUM(RECEIVABLE_CREATED)`. Comparison = AR-scoped `payment_allocations` (only rows whose `invoice_id` has a matching `RECEIVABLE_CREATED` event) + `SUM(BAD_DEBT_WRITE_OFF)` + outstanding `ISSUED`/`PART_PAID` invoice.total − allocations. Empty ISSUED/PART_PAID set → COALESCE fallback to the Stage 9 residual so prepaid/accounting worlds stay GREEN. Prepaid invoice cash is settlement, not AR. | F vs Stage 9 restatement / payments landing | Stage 10 |
| 2026-08-20 | DL-139 | Sibling runner `ERROR_CODES` drop R042, R044, R049, R053 in `runner.test.js`, `runner-funded-green`, `runner-postpaid-green`, `runner-accounted-green`, `runner-rated-green`, `runner-priced-green`, `runner-metered-green`. Those checks were ERROR on `42P01` (missing `billing_periods` / `invoice_lines` / `invoices`). Empty worlds are now GREEN (R042 OPEN_PERIOD + NULL period; R044/R049/R053 empty-empty). `r040-r046` R042 ERROR→GREEN; `r040-r049` R042/R044/R049 ERROR→GREEN; `r050-r053` R053 ERROR→GREEN. | Stage 10 tables vs DL-129 | Stage 10 |
| 2026-08-20 | DL-140 | R070–R073 implement the Stage 10 plan restatement (issued invoice totals, sequence assignment, allocation vs status, unapplied_cash identity), not F's original R070–R073 pair. R070 comparison is `lines + tax_lines + signed adjustments` vs frozen `total_minor` (invoice_number NOT NULL, including VOID). Credit notes (negative adjustments) will DRIFT R070 against immutable totals — that is the detective; do not mutate `total_minor` after ISSUE. | F § vs user Stage 10 restatement (mirrors DL-127) | Stage 10 |
| 2026-08-20 | DL-141 | `WriteOffInvoice` flips the invoice `ISSUED`/`PART_PAID` → `UNCOLLECTIBLE` after `recordCreditLoss`, so R061 outstanding AR does not double-count the write-off. Audit already named `INVOICE_UNCOLLECTIBLE`. | R061 + B §16 | Stage 10 |
| 2026-08-20 | DL-142 | `fin.tax_registrations`, payment_methods, disputes, `/api/admin/fin/**`, vendor economics (Stage 11), ops UI (Stage 12), and commercial.* cutover (Stage 13) are not built here. IssueInvoice still uses Stage 9 MANUAL `resolveTax` (`OUT_OF_SCOPE`, vat_bps=0, DL-126). PDF/ZATCA HTTP stay outbox (`fin.invoice.render` / `fin.zatca.submit`). | Do not half-build later stages | Stage 10 |
| 2026-08-20 | DL-143 | Full `ReversePayment` (allocated → 0) restores invoice `PAID` → `ISSUED`. Migration 201's status-flip only allowed `PAID` → `PART_PAID`. Additive migration 205 `CREATE OR REPLACE FUNCTION fin.trg_invoices_status_flip()` adds that one `ELSIF`; 201 is not edited. | B §16 silent on full reversal; audit of PR #15 | Stage 10 |
| 2026-08-20 | DL-144 | `refundPurchase` consumed-then-refund fires `REFUND_REVENUE_REVERSED` even when PURCHASE lots are exhausted. Stage 9 stamps `REVENUE_RECOGNIZED` as `RATED_USAGE` (DL-128); recognized lookup now includes that join plus the allocation-line accumulator. `evaluateRefund` is not gated on `remaining_units`. | C §5.7 / audit of PR #15 | Stage 10 |
| 2026-08-20 | DL-145 | `advanceBillingPeriodClose` returns the billing-period row (`status` = period status). Tax checklist steps 5–7 are no-ops (tax is snapshotted at ISSUE); `INVOICE_DRAFTED`+`DRAFT` jumps to approve so the walker cannot stall returning an invoice `DRAFT`. | C §5.6 / audit of PR #15 | Stage 10 |
| 2026-08-20 | DL-146 | runner-billed-green `INVOICE_NOT_DRAFT` with `status: DRAFT` was applyPayment correctly rejecting a never-issued invoice (period-close stall, DL-145). Guard is not inverted; DRAFT is not added to the INSERT trigger allow-list. Walk now ISSUEs before pay. | Audit of PR #15 | Stage 10 |
| 2026-08-20 | DL-147 | Parallel per-test-DB creation races Stage 1 `109` `CREATE ROLE` on cluster-global `pg_authid` (TOCTOU). Additive 206 is an idempotent no-op once roles exist. `createTestDatabase` retries `runMigrations` on `23505` / `pg_authid_rolname_index` because 109 still runs first. Stage 1 files stay frozen. | Test infra / audit of PR #15 | Stage 10 |
| 2026-08-20 | DL-148 | `writeInvoiceStatus` dedupe is `inv:{id}:{to}:{version}`. Callers must pass the post-flip invoice (`UPDATE … RETURNING *`) because `trg_bump_version` increments during the UPDATE. A stale pre-flip object reuses the ISSUE-time version when `ReversePayment` restores `ISSUED` (DL-100 pattern). | Audit of PR #15 follow-up | Stage 10 |
| 2026-08-20 | DL-149 | R061 outstanding sum is AR-scoped (`EXISTS RECEIVABLE_CREATED` on the invoice). Outer COALESCE is `0`, not the Stage 9 residual that turned prepaid PAID cash into `-allocated`. Prepaid invoice cash is settlement, not AR (DL-138). BAD_DEBT is AR-scoped the same way so prepaid write-offs do not inflate comparison. `RECEIVABLE_CREATED` whose `source_id` is not an invoice (facility reservation) stays in comparison so Stage 9 postpaid worlds remain 0-delta. | Audit of PR #15 follow-up | Stage 10 |
| 2026-08-20 | DL-150 | `writeInvoiceStatus`'s `notification.lifecycle` dedupe was missed by DL-148 — the version was only added to the `fin.invoice.status` topic. Reverse-full (PAID → ISSUED) collided on `inv:{id}:ISSUED:notify`. Version tail added: `inv:{id}:{to}:notify:v{version}`. Same DL-100 / DL-148 pattern. | Follow-up #3 audit | Stage 10 |
| 2026-08-21 | DL-151 | Advisory lock `FIN_VENDOR_STATEMENT_RECON = 1021`. Per-statement `pg_advisory_xact_lock(1021, hashtext(statementId))` inside `transaction(fn)` during the 6-way recon and FINALIZE. No book lock — vendor economics are metrics, not ledger. Per-vendor commands take `pg_advisory_xact_lock(hashtext('vendor:'||id))` plus `FOR UPDATE` on the vendor header. Stage 12 uses 1030+. | D §7.1 / user Stage 11 restatement | Stage 11 |
| 2026-08-21 | DL-152 | `fin.vendor_variance_reasons` landed as a **TABLE** (10 PK codes), not B §23 error codes. The 10 reasons are data classifications on variance rows, not exceptions. Command errors for illegal transitions stay FinError codes (`VENDOR_STATEMENT_UNRESOLVED_VARIANCE`, …). | A §11 / user Stage 11 restatement | Stage 11 |
| 2026-08-21 | DL-153 | Stage 5 `rateMeteredUsage` writes `fin.vendor_cost_estimates` in the same tx (ALS / D-T11) when a mapping exists. Silent skip when none. Mapping source is `fin.meter_vendor_map` — Stage 3 meters have `filter_definition`, not `dimensions`. | A §11.6 / Stage 3 schema | Stage 11 |
| 2026-08-21 | DL-154 | `fin.accounting_events.event_kind` CHECK extended in migration 213 (DROP + ADD) to include `PROVIDER_COST_ATTRIBUTED`, preserving every prior enum value. `source_type` similarly gains `VENDOR_ACTUAL_COST`. Stage 9 migrations and `evaluatePostpaidCapture` / `evaluateConsumption` are not edited. JS twin is `accounting/helpers.js` EVENT_KINDS / SOURCE_TYPES. | Additive enum extension | Stage 11 |
| 2026-08-21 | DL-155 | `PROVIDER_COST_ATTRIBUTED` fires **per `vendor_actual_cost` row** when the parent `vendor_statement` is FINALIZED (`source_type=VENDOR_ACTUAL_COST`, `source_id=actual.id`). Rows that cannot resolve tenant + billing_account + legal_entity via `rated_usage` are skipped; R083 then DRIFTs — tests always attribute. | spec §125 margin / R083 | Stage 11 |
| 2026-08-21 | DL-156 | Margin math NEVER touches `lot.remaining_units`. `contributionMargin` / `computeMargin` use `REVENUE_RECOGNIZED` amounts minus `vendor_actual_costs` joined to `rated_usage` then `revenue_allocation_lines` (DL-128). Enforced by `margin-not-conflated.test.js`. | spec §125 | Stage 11 |
| 2026-08-21 | DL-157 | Ten variance reason codes registered: `drift`, `rate_change`, `late_usage`, `duplicate`, `missing_source`, `timezone`, `rounding`, `currency_mismatch`, `classification_drift`, `unknown`. | DL-152 table | Stage 11 |
| 2026-08-21 | DL-158 | Stage 11 restates B §21 `OPEN/FINALIZED` as `DRAFT → RECEIVED → RECONCILED → FINALIZED` (no reopen after FINALIZED). Rate versions use `DEPRECATED` (not Stage 4 `SUPERSEDED`); gap-fill of prior ACTIVE `effective_to` is application-side like 117. Catalog tables without `tenant_id` use catalog RLS (`USING true`) matching 115 prices; `vendor_usage_events` uses tenant isolation with NULL tenant allowed. `vendor_reported_usage` is a MUTABLE header (A §11.5 said APPEND_ONLY). `vendor_actual_costs.vendor_statement_line_id` FK is added in 212. | User Stage 11 restatement (mirrors DL-140) | Stage 11 |
| 2026-08-21 | DL-159 | R080–R083 implement the Stage 11 plan restatement (actuals vs statement total; ACTIVE estimate has a covering rate version; FINALIZED has zero unresolved variance; PROVIDER_COST_ATTRIBUTED sum vs FINALIZED actuals), not F's original R080–R083 (composite FK / estimate-before-actual / line-sum / overlap). Empty worlds are GREEN so sibling runners stay green without vendor seed. | F §13 vs user Stage 11 restatement | Stage 11 |
| 2026-08-21 | DL-160 | Spec §130 E2E traversal uses **vitest + jsdom** for the 15 admin pages and **real-Postgres vitest** (`backend/src/e2e/admin-fin-traversal.test.js`) for the four SQL join paths + GET sweep. Playwright is not a `web/package.json` dependency; introducing it in Stage 12 would be a new UI test framework (out of scope). | Stage 12 plan: pick Playwright vs jsdom | Stage 12 |
| 2026-08-21 | DL-161 | DL-101 **addressed**. `funding/http.js` no longer reads `environment` or `now` from `req.body`. Environment is `req.user.fin_environment \|\| req.user.environment \|\| LIVE`; time is `BusinessClock.now()`. | Stage 8 / Stage 7 reserved hardening | Stage 12 |
| 2026-08-21 | DL-162 | DL-102 **addressed** via additive migration `220_fin_ledger_books_customer_unique.sql`: `UNIQUE (billing_account_id, environment) WHERE book_type = 'CUSTOMER'`. Coexists with Stage 1 `UNIQUE (environment, billing_account_id, book_type)`. No advisory lock. | Stage 7 reserved hardening | Stage 12 |
| 2026-08-21 | DL-163 | **No** materialized views for spec §103 KPIs. The 24 tiles are live SELECTs; a failed SELECT becomes `0` rather than inventing a substitute metric. Refresh-on-demand POST is not required. Migration 221 not landed. | Stage 12 plan optional MV 221 | Stage 12 |
| 2026-08-21 | DL-164 | Admin ops routes (`fin/admin/routes.js` + `context.js`) take `environment` from operator session (`fin_environment` / `environment` / LIVE) and `now` from `BusinessClock`. `req.body.environment` / `req.body.now` are stripped. `actorType='USER'`; `actorId` is set only when `req.user.id` is a UUID (TEXT user ids cannot land in UUID actor columns). Idempotency-Key header is passed through. | Mirrors DL-101 for the admin surface | Stage 12 |
| 2026-08-21 | DL-165 | `resolveDrift` is not a domain command. `POST /api/admin/fin/reconciliation/drift/:id/resolve` returns **501** `{ dl: 'DL-165' }`. Read-side exception types without a command: `USAGE_DLQ`, `AUTH_DENIED`, `HOLD_EXPIRED`, `LATE_USAGE`, `ENV_ISOLATION`, `IDEMPOTENCY_IN_FLIGHT`, `ACCOUNTING_HARD_CLOSED`. Do not invent writers here. | Spec §107 / never invent a domain command | Stage 12 |
| 2026-08-21 | DL-166 | `approveRequest` / `rejectRequest` (`DecideApproval`) are not built. `POST /api/admin/fin/approvals/:id/approve` and `/reject` return **501** `{ dl: 'DL-166' }`. `APPROVAL_PENDING` is read-only until a later stage owns the checker command. | B_STATE_MACHINES named DecideApproval; no service | Stage 12 |
| 2026-08-21 | DL-167 | Vendor exception types `VENDOR_STATEMENT_DRIFT` and `NEGATIVE_MARGIN` return count `0` and are marked deferred until Stage 11's vendor tables/routes merge. Stage 12 imports `./vendors/routes.js` when present; otherwise GET `/api/admin/fin/vendors` is a 200 stub `{ stage11: false }`. Do not redefine Stage 11 READ routes. | Parallel Stage 11 branch | Stage 12 |
| 2026-08-21 | DL-168 | `RATE_NOT_CONFIGURED` has no domain command and no dedicated queue table. Exception tile count is `0`; there is no write route. Rating denials stay on Stage 5 `FIN_NO_ACTIVE_PRICE` (DL-081). | Spec §107 | Stage 12 |
| 2026-08-21 | DL-169 | `TAX_MISMATCH` has no domain command (Stage 9 `resolveTax` is MANUAL / `OUT_OF_SCOPE`, DL-126). Exception tile counts R073 DRIFT rows when present; no write route. | Spec §107 / tax registrations later | Stage 12 |
| 2026-08-21 | DL-170 | `POST /billing/periods/:id/reopen` reads period status and returns **409** `BILLING_PERIOD_ALREADY_OPEN` when already `OPEN`. Stage 10 `reopenBillingPeriod` already throws `BILLING_PERIOD_SKIP` for OPEN (not a silent no-op; admin `wrap` already maps FinError → 4xx). The PR #17 200 was reopen after a successful close (`USAGE_CLOSING` → `OPEN` is legal). Guard is route-level because Stage 10 files are frozen; no new command. PR #17 named this DL-166; that row already records DecideApproval 501s. | PR #17 audit follow-up | Stage 12 |
| 2026-08-21 | DL-190 | (was DL-160) Additive migration `214` DROP+ADD `chk_approval_requests_action_kind` to include `VENDOR_VARIANCE_OVERRIDE`, preserving every Stage 1 value from 106. FINALIZE-with-override inserts this kind via `insertApproval`. 106 is not edited. Dispatch labelled this DL-158; that id was already consumed by the Stage 11 restatement row. Renumbered on merge with main so Stage 12's DL-160..DL-170 stay canonical. | Audit of PR #16 | Stage 11 follow-up |
| 2026-08-21 | DL-191 | (was DL-161) `upsertVendorProduct` uses Semantic A (Stage 4/7 version-aware pattern): default key is `VENDOR_PRODUCT:UPSERT:{vendorId}:{productCode}:{payloadHash}`. Same payload replays; a different payload (e.g. product_class) is a new claim then INSERT ON CONFLICT UPDATE. Semantic B (stable key, delete-and-reclaim on fingerprint mismatch) was rejected so claim() stays fingerprint-strict. Dispatch labelled this DL-159. | Audit of PR #16 | Stage 11 follow-up |
| 2026-08-21 | DL-192 | (was DL-162) `deprecateRateVersion` flips status only and does **not** write `effective_to`. Gap-fill of the prior ACTIVE already recorded the correct close when the successor activated (`deprecateActiveRateVersion`). Setting `effective_to = now` reversed tstzrange when the ACTIVE row's `effective_from` was in the future. Dispatch labelled this DL-160. | Audit of PR #16 | Stage 11 follow-up |
| 2026-08-21 | DL-193 | (was DL-163) `rate-effective-date.test.js` seeds `metered_usage.metered_at` at INSERT via `seedRatedCase({ now })` → `meterPeriod`'s `now` column. Stage 3's append-only trigger correctly forbids post-INSERT UPDATE of `metered_at`. Helper-only; metering service unchanged. Dispatch labelled this DL-161. | Audit of PR #16 | Stage 11 follow-up |
| 2026-08-21 | DL-194 | (was DL-164) 6-way recon coerces both sides with `asMinor` (BIGINT) before compare. Empty-vs-empty (0=0) writes no row. Legs B (metered_usage vs vendor_usage_events) and E (estimates vs actuals) were the two false-positives on a matching vendor-only statement: platform-side left was 0, E was auto-hinted `rate_change`. Skip A/B/E when the platform/estimate left is 0 unless an explicit hint.reason is set. Dispatch labelled this DL-162. | Audit of PR #16 | Stage 11 follow-up |
| 2026-08-21 | DL-195 | (was DL-165) `attributeProviderCostForStatement` joined `billing_accounts` on `(holder_id, environment)`, so one `vendor_actual_cost` fanned out across every currency account on the holder; the second INSERT hit `uq_accounting_events_provider_cost` (23505), the catch continued without a savepoint, and the next `loadActivePolicy` saw `25P02`. Join is now `rated_usage → contract_versions → contracts.billing_account_id` (1:1); FINALIZE retry skips via SELECT, not by swallowing 23505. | Audit of PR #16 follow-up #2 | Stage 11 follow-up |




| 2026-08-21 | DL-171 | Stage 13a un-freezes `billing/events.js` and legacy write paths for dual-write only. Legacy remains source of truth in DUAL mode. `emitUsageEvent` wraps legacy + fin mirror in one `transaction(fn)` when mode is DUAL/FIN_ONLY (closes A/B-1 for allowlisted tenants). | Stage 13 step 1 / I-14 | Stage 13a |
| 2026-08-21 | DL-172 | `FIN_CUTOVER_MODE_GLOBAL` env flag: `OFF` (default) / `DUAL` (per-tenant allowlist) / `FIN_ONLY` (global). Global `FIN_ONLY` wins; otherwise `fin.cutover_tenant_allowlist.mode='DUAL'`; else `OFF`. Per-tenant rows never store `FIN_ONLY`. | Cutover gate | Stage 13a |
| 2026-08-21 | DL-173 | Dual-write failures go to append-only `fin.cutover_dual_write_errors`. Legacy write is NEVER blocked by a fin.* failure (SAVEPOINT + DLQ insert). No outbox row on failure. | Additive dual-write | Stage 13a |
| 2026-08-21 | DL-174 | R084 monitors dual-write error rate: DRIFT when `COUNT(*)` of errors with `occurred_at > now()-24h` is `>= 100`. Severity MEDIUM, `drift_action WARN`. (F's original R084 vendor A/B is deferred; vendor recon stays with Stage 11/13c.) Recon role may SELECT the DLQ table. | Operational guard | Stage 13a |
| 2026-08-21 | DL-175 | Translator `usageEventInput`: `source_system='commercial.usage_events'`, `source_event_id=legacy.id`, `event_type=action_key`, `quantity_units=quantity` 1:1 (scale refinement deferred to 13b parity). | Pure mapping | Stage 13a |
| 2026-08-21 | DL-176 | Translator `holdAuthorizeInput`: maps hold-shaped payloads to `authorizeUsage`. No `commercial.holds` writers exist today; translator is ready for 13b/backfill. | Pure mapping | Stage 13a |
| 2026-08-21 | DL-177 | Translators `captureUsageInput` / `captureFacilityInput` for capture-shaped payloads. No commercial capture writers today. | Pure mapping | Stage 13a |
| 2026-08-21 | DL-178 | Translator `refundPurchaseInput` for refund/credit-note shaped payloads toward Stage 10 `refundPurchase`. Commercial credit-notes are not wired in 13a. | Pure mapping | Stage 13a |
| 2026-08-21 | DL-179 | `commercial.ledger_entries` consumption dual-writes via `ledgerConsumptionAuthorizeInput` -> `authorizeUsage`. Missing fin mirror context / book / funded lots land in the DLQ; legacy consumption still commits. | Translator choice | Stage 13a |
| 2026-08-22 | DL-180 | Backfill cutoff is `MIN(fin.usage_events.received_at)` for DUAL-allowlisted tenants where `source_system='commercial.usage_events'` (the DUAL-write natural key from DL-175). Backfill never writes rows with `occurred_at`/`created_at` ≥ that cutoff, so history and DUAL do not overlap. A source whose cutoff has not been established (no allowlist and `FIN_CUTOVER_MODE_GLOBAL` is not `FIN_ONLY`, or no DUAL-originated fin row yet) is refused. | Stage 13 step 2 | Stage 13b |
| 2026-08-22 | DL-181 | No new columns on `fin.usage_events` — Stage 2 already has `source_system` + `source_event_id` with UNIQUE `(environment, source_system, source_event_id, residency_key)`. Backfill uses `source_system='commercial'` and `source_event_id=legacy.id` (distinct from DUAL's `commercial.usage_events`) plus `ON CONFLICT DO NOTHING`. `fin.rated_usage` and `fin.accounting_events` gain nullable `source_system`/`source_row_id` with partial UNIQUE indexes for consumption-backfill idempotency. | Stage 2 schema reuse | Stage 13b |
| 2026-08-22 | DL-182 | `fin.cutover_backfill_progress` and `fin.cutover_backfill_corrections` are APPEND_ONLY (`REVOKE UPDATE, DELETE, TRUNCATE`). Each batch inserts a start row and a completion row sharing `batch_id`; `last_processed_at` is never UPDATEd in place. Concurrent exclusion is advisory lock `FIN_CUTOVER_BACKFILL = 1030` (start rows keep `completed_at` NULL forever, so a live unique on in-flight source would admit only one run for the life of the table). | APPEND_ONLY progress | Stage 13b |
| 2026-08-22 | DL-183 | Go/no-go for Stage 13d: R090, R091, and R092 all GREEN and R084 24h dual-write errors `< 100`. R090 HIGH `BLOCK_CUTOVER` (env not LIVE/TEST). R091 CRITICAL `BLOCK_CUTOVER` (accounting_events tenant ≠ usage_events tenant on `source_type='RATED_USAGE'`). R092 HIGH `BLOCK_CUTOVER` (invoice `legal_entity_id` ≠ billing_account seller or ≠ rated_usage→contract seller). Additive; empty worlds stay GREEN. | Stage 13 step 4 | Stage 13b |
| 2026-08-22 | DL-184 | `GET /api/admin/fin/cutover/readiness` is a read-only platform_admin JSON summary (dual-write 24h error count, R090–R092, backfill progress, corrections_total, `ready_for_cutover`). Not a new admin page. | Stage 12 admin surface | Stage 13b |
| 2026-08-22 | DL-185 | Progress rows carry `last_processed_id` so same-timestamp chunks resume on `(occurred_at, id)` / `(created_at, id)`. Correction kinds are the 241 CHECK list; `UNIQUE (source, source_row_id, correction_kind)` makes correction inserts idempotent. | Operator cursor / taxonomy | Stage 13b |
| 2026-08-22 | DL-186 | Reserved — correction taxonomy extensions. | Stage 13b reserved | Stage 13b |
| 2026-08-22 | DL-187 | Reserved — operator ops / backfill window tooling. | Stage 13b reserved | Stage 13b |
| 2026-08-22 | DL-188 | Reserved — operator ops / backfill window tooling. | Stage 13b reserved | Stage 13b |
| 2026-08-22 | DL-189 | Reserved — operator ops / backfill window tooling. | Stage 13b reserved | Stage 13b |
| 2026-08-22 | DL-196 | Advisory lock `FIN_CUTOVER_PARITY = 1031`. Session-scoped for a parity tick (the tick spans multiple short transactions, so xact-scoped would not cover the window). `key2 = hashtext(source)` so usage_events / ledger_entries / holds / captures may run in parallel. DL-190..DL-195 were consumed by Stage 11 follow-up on merge, so Stage 13c starts at DL-196. | Stage 13 step 3 | Stage 13c |
| 2026-08-22 | DL-197 | `fin.cutover_parity_reports` UNIQUE `(environment, source, window_start, window_end)`. Worker accumulates in memory and INSERTs the final row `ON CONFLICT DO NOTHING` (the original stands). Crash mid-window writes no report; the next tick redoes the window. APPEND_ONLY via trigger + REVOKE. | Idempotent burn-in | Stage 13c |
| 2026-08-22 | DL-198 | `attestation_hash` = SHA-256 of canonical JCS over `{environment, first_green_at, last_green_at, reports: [id...], total_rows_checked, total_rows_drifted, outstanding_corrections}`. Signer email is captured; UNIQUE `(environment, attestation_hash)` makes re-sign against the same evidence a dedupe. | Finance sign-off | Stage 13c |
| 2026-08-22 | DL-199 | 30-day burn-in threshold is hard-coded for LIVE readiness (`burn_in_days_required: 30`). CLI `--burn-in-days` overrides for staged rollouts. | Plan §Stage 13 step 3 | Stage 13c |
| 2026-08-22 | DL-200 | R093 threshold is 50 bps (0.5%) on the latest daily report per source (HIGH, `BLOCK_CUTOVER`). R094 requires continuous GREEN daily reports among sources that have reports (HIGH, `BLOCK_CUTOVER`); empty tables are GREEN. R095 is informational (`WARN`) when backfill corrections trend up while burn-in is running. | Cutover recon | Stage 13c |
| 2026-08-22 | DL-201 | `ready_for_cutover` is the AND of six signals: R090 GREEN, R091 GREEN, R092 GREEN, R093 GREEN, R094 GREEN, R084 24h errors `< 100`, and an attestation `last_signed_at` within 7 days. Documented on `GET /api/admin/fin/cutover/readiness`. | Stage 13d gate | Stage 13c |
| 2026-08-22 | DL-202 | Usage-event parity joins `fin.usage_events` on `source_event_id = legacy.id` and `source_system IN ('commercial', 'commercial.usage_events')` so the worker observes both the 13b backfill natural key (DL-181) and the 13a DUAL key (DL-175). Consumption joins `source_system='commercial.ledger_entries'`. | Dual-origin mirrors | Stage 13c |
| 2026-08-22 | DL-203 | Empty parity tables are GREEN for R093–R095 (no evidence of failure). `eligible_to_sign` / `burn_in_met` still require 30 consecutive GREEN daily reports. This keeps sibling runner-*-green fixtures green. | Empty-world default | Stage 13c |
| 2026-08-22 | DL-204 | Reserved — Stage 13c follow-up. | Stage 13c reserved | Stage 13c |
| 2026-08-22 | DL-205 | Reserved — Stage 13c follow-up. | Stage 13c reserved | Stage 13c |
| 2026-08-22 | DL-206 | Migration 260 REVOKEs `INSERT, UPDATE, DELETE, TRUNCATE` on every `commercial.*` table discovered from `pg_tables` (plus any extra non-superuser writer role). SELECT is kept. `fin_migrator` is never revoked. Paired down-migration `260b_fin_cutover_thaw_commercial.sql` re-GRANTs INSERT/UPDATE/DELETE to the same named roles and is skipped by `runMigrations` (`NNN[letter]_*.sql`). | Stage 13 step 6 | Stage 13d |
| 2026-08-22 | DL-207 | Cutover mode is resolvable via `fin.cutover_active_environment` (seeded `OFF`). `FIN_ONLY` on that row short-circuits `resolveCutoverMode`. `OFF`/`DUAL` fall back to `FIN_CUTOVER_MODE_GLOBAL` + allowlist. Env var `FIN_CUTOVER_MODE_GLOBAL=FIN_ONLY` still works. Activation uses DELETE-and-INSERT of the singleton row rather than in-place UPDATE (small append-only hack; `updated_at` is the clock). | Operator flip without redeploy | Stage 13d |
| 2026-08-22 | DL-208 | R096 (attestation freshness) is CRITICAL / `BLOCK_NEW_ISSUANCE`. DRIFT when any env is `FIN_ONLY` without a referenced attestation signed within 7 days. Empty/OFF/DUAL worlds are GREEN. Wired into `resolveGlobalCutoverMode` as fail-closed defense. | Cutover recon | Stage 13d |
| 2026-08-22 | DL-209 | Startup gate refuses to boot when `FIN_ONLY` (env var or DB row) has no fresh attestation. `FIN_CUTOVER_SKIP_ATTESTATION_GATE` is a local-dev bypass; PROD deploys MUST NOT set it. | Fail-closed boot | Stage 13d |
| 2026-08-22 | DL-210 | `fin_public.*` views redirect reads at the SQL layer (`security_invoker=true` so FORCE RLS on underlying `fin.*` continues to apply). Views: `usage_events` → `fin.usage_events`; `ledger_entries` → `fin.rated_usage` consumption mirror. Application code still reads `commercial.*` until 13e/13f. Commercial-only (no view): `territories`, `pricing_zones`, `pricing_cities`, `core_rate_cards`, `billing_products`, `billing_product_tiers`, `billing_product_territory_pricing`, `billing_subscriptions`, `billing_subscription_history`, `billing_credit_notes`, `notification_events`, `notification_deliveries`, `notification_preferences`, plus `usage_events` partitions. `commercial.holds` / `commercial.captures` are parity sources, not tables. | Read redirect | Stage 13d |
| 2026-08-22 | DL-211 | Rollback: `POST /api/admin/fin/cutover/deactivate` (mode → `DUAL`, not `OFF`) plus manual apply of migration 260b. Documented in `docs/ops/CUTOVER_13D_RUNBOOK.md`. No commercial.* DROP/ALTER/DELETE in 13d. | Rollback-safe cutover | Stage 13d |
| 2026-08-22 | DL-212 | Reserved — Stage 13d follow-up. | Stage 13d reserved | Stage 13d |
| 2026-08-22 | DL-213 | Reserved — Stage 13d follow-up. | Stage 13d reserved | Stage 13d |
| 2026-08-22 | DL-214 | Reserved — Stage 13d follow-up. | Stage 13d reserved | Stage 13d |
| 2026-08-22 | DL-215 | Reserved — Stage 13d follow-up. | Stage 13d reserved | Stage 13d |
| 2026-08-22 | DL-216 | Freeze migration renamed `260a_fin_cutover_freeze_commercial.sql` and skipped by `isAutoMigration` (matches the `NNN[letter]_*.sql` rule that already excludes `260b` thaw). Railway auto-deploys merges to `main` and `scripts/start.js` runs `runMigrations()` on every boot — auto-applying an unconditional REVOKE would flip production the moment PR #21 merges, breaking every legacy tenant still in OFF mode. New endpoint `POST /api/admin/fin/cutover/freeze-commercial` applies the migration on operator command AFTER `/activate` flips the singleton to `FIN_ONLY`. `freezeCommercialWrites` in `activation.js` reads the SQL file from disk (single source of truth), refuses when singleton is not `FIN_ONLY`, writes an audit row + `fin.cutover.commercial_frozen` outbox event. Runbook updated with the new step 8. DL-206 stands as the freeze semantics; DL-216 adjusts the trigger from "auto-migration on deploy" to "operator command after activate". | Audit of PR #21: Railway auto-deploy would fire the freeze before the operator was ready. | Stage 13d |


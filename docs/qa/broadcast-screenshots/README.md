# Broadcast theme screenshots

Light and dark captures of the top traffic surfaces for architect-owner review.

Authenticated routes (`/dashboard`, `/listings`, `/inbox`, `/contacts`, `/settings`, `/command-center`) render the signed-out gate in this preview (no API session). Login and Register are the full painted screens; gated routes still exercise the shell, tokens, and `[data-lc-mode]` switch.

Captured 2026-09-04 against `vite preview` of this PR build.
